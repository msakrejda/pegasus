package org.postgresql.febe.message {

    import org.postgresql.febe.message.IBEMessage;
    import org.postgresql.io.ICDataInput;

    public class NoticeResponse extends ResponseMessageBase implements IBEMessage {
        /* nothing to do here: this is essentially a marker class */
    }
}